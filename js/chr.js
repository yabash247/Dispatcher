
//create / edit / update mysql  database (help needed)

//get load search settings data from json file and popup.html form 

//set search details in website (https://www.navispherecarrier.com/)

//If user is available to work (per time.json or popup'html form)
//if available keep runing below load search at intervals 

    // click on search buttion on website 

    // get search standard for each availability 

    // for each search result 

    // if millage <= max_mill 

        // get Pickup deadhead proximiy 
        // get delievry deadhead proximity 
        // get pay 
        // get number of shits if (millage <= 60) ? 2 : 1


        //If load type == "great"
        //if (deadhead_pu == close and deahead_del == far || close AND ((PAY => 250 AND nos_shit == 2) OR (PAY => 600 AND nos_shit == 1)) 
            // if (PAY => 250 AND nos_shit == 2) 
                //bid the greater of (pay*1.3 or 300)
                //if not approved , bid the greater of (pay*1.2 or 300)
                // if approved or pay 
                //Book load
                //remove particular carrier seeting to avoid double booking (help needed)






